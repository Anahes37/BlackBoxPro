# BlackBoxPro 测试模式说明

本文档用于明确 BlackBoxPro 当前的两类测试模式，避免把 `plugin + 客户端联合测试` 和 `纯客户端测试` 混在一起执行。

## 1. 结论先行

当前测试模式拆分为两类：

1. `plugin + 客户端联合测试`
2. `纯客户端测试`

本次 `neoforge-1.21.1` 的验证归类为 `纯客户端测试`，不依赖 Bukkit/Paper `plugin`，也不走 Plugin Message 联调链路。

## 2. 两类模式的边界

| 模式 | 目标 | 是否依赖服务端 Plugin | 是否依赖客户端 Mod | 通讯方式 | 当前主要版本 |
|------|------|----------------------|-------------------|----------|--------------|
| `plugin + 客户端联合测试` | 验证服务端插件、Plugin Message、客户端执行链路整体联通 | 是 | 是 | `blackbox:command` / `blackbox:response` | `1.21.11`、`1.12.2` |
| `纯客户端测试` | 验证客户端本地 Action、世界管理、查询、单机状态切换 | 否 | 是 | 本地 HTTP API | `neoforge-1.21.1` |

## 3. plugin + 客户端联合测试

### 3.1 适用场景

- 验证 `plugin` 模块的 API、命令、回调、超时和截图流程
- 验证服务端插件通过 Plugin Message 控制客户端执行 Action
- 验证 `blackbox test <player>` 这类完整集成测试

### 3.2 典型特征

- 需要启动服务端
- 需要部署 `plugin` 产物到服务端
- 需要部署客户端 Mod 到实际客户端目录
- 需要玩家进入服务端
- 测试入口通常是服务端命令或 `plugin` API

### 3.3 当前归档版本

- `1.21.11`
- `1.12.2`

### 3.4 不属于本模式的内容

- `runClient` 本地开发启动
- 本地单机世界 `create_world / join_world / leave_world`
- 直接调用 `127.0.0.1:25580` HTTP 接口

## 4. 纯客户端测试

### 4.1 适用场景

- 验证客户端本地 Action 是否工作
- 验证单机世界管理流程
- 验证纯查询类 Action 是否能在本地世界或主菜单下正确回报
- 验证客户端从世界返回主菜单、重新进入世界等状态切换

### 4.2 当前测试线

当前纯客户端测试线为：

- 模块：`neoforge-1.21.1`
- 启动方式：`.\gradlew :neoforge-1.21.1:runClient`
- 本地 HTTP：`http://127.0.0.1:25580/api/command`
- 运行目录：`neoforge-1.21.1/run`

### 4.3 当前覆盖内容

当前已明确归入纯客户端测试的能力包括：

- `create_world`
- `join_world`
- `leave_world`
- `query_player_state`
- `query_world_state`
- 其他本地可验证的客户端 Action

### 4.4 典型特征

- 不启动 Bukkit/Paper 服务端
- 不部署 `plugin` 到服务端
- 不依赖玩家进入外部服务器
- 由本地 `runClient` 拉起 NeoForge 客户端
- 通过 HTTP 直接下发 JSON 指令并读取响应

### 4.5 当前脚本归属

以下脚本应视为纯客户端测试脚本，而不是联合测试脚本：

- `test_world.sh`
- `test_socket.py`

## 5. 模式选择规则

当需求描述中出现以下关键词时，应优先判定为 `plugin + 客户端联合测试`：

- `联调`
- `plugin`
- `服务端`
- `blackbox test`
- `Plugin Message`
- `Bukkit/Paper`

当需求描述中出现以下关键词时，应优先判定为 `纯客户端测试`：

- `纯客户端`
- `纯 mod`
- `runClient`
- `本地单机`
- `世界管理`
- `HTTP`
- `create_world`
- `join_world`
- `leave_world`

如果用户没有明确说明模式，必须先结合目标模块和测试入口判断，不要默认把所有测试都当成服务端联调。

## 6. 执行约束

### 6.1 联合测试约束

- 改动 `plugin` 后必须重启服务端
- 改动客户端 Mod 后必须重启客户端
- 联合测试的通过标准是：服务端和客户端两侧链路都正常

### 6.2 纯客户端测试约束

- 不要求启动服务端
- 不要求存在 `plugin` 产物
- 通过标准是：客户端本地状态切换与 HTTP 回包正确
- `leave_world` / `join_world` 这类 Action 必须基于单机世界流程验证
- 世界管理 Action 必须保持原子语义：不能在“命令已发出”时就回 `success`
- `create_world` 只有在客户端真正进入新世界后才能 `success`
- `join_world` 只有在目标世界真实加载完成后才能 `success`；如果世界不存在或缺少 `level.dat` / `level.dat_old`，必须直接 `failure`
- `leave_world` 只有在客户端真正回到主菜单后才能 `success`
- 纯客户端回归脚本不应依赖固定 `sleep` 来等待世界切换完成；若需要固定等待，说明 Action 完成语义不完整

## 7. 当前实践结论

从现在开始：

- `1.21.11`、`1.12.2` 默认按 `plugin + 客户端联合测试` 处理
- `neoforge-1.21.1` 当前按 `纯客户端测试` 处理
- 后续若为 `1.21.11` 增加本地 HTTP / 单机测试链路，应单独登记，不与现有联合测试混用

## 8. 后续工作

本文档落地后，后续需同步更新：

- `README.md`
- `CLAUDE.md`
- `blackbox-runner` skill

使工具和文档在“测试模式”这件事上保持同一套语义。
